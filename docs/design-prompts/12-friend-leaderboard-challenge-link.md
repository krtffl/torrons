# Prompt 12 — Friend leaderboard & challenge link

**Set:** B (new screens for shipped features)
**Status:** Live at `GET /friends`, `GET /friends/join/{inviteCode}`, `GET /friends/{circleId}` (`public/templates/friends.html`). Shipped 2026-07-03.

## Prompt (copy-paste this into Claude Design)

```
Torrorèndum: Catalan voting competition for Torrons Vicens, warm brown/gold/cream palette, burgundy accent. Design two connected moments: (1) a "reptar un amic" share screen where a user generates a personal challenge link, with a short, confident headline framing it as a direct challenge, not a generic invite; and (2) the friend-scoped leaderboard that results from it — same visual language as the main leaderboard (Prompt 05) but visibly smaller and more personal, showing only the user and whoever has joined via their link, with clear affordance to invite more people into this specific circle.
```
