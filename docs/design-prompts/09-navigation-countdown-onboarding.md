# Prompt 09 — Navigation, countdown & onboarding

**Set:** A (existing site, redesigned)
**Status:** Nav is live in the shared `header`/`topbar` partials (`public/templates/index.html`); the countdown is live at `GET /countdown` and `GET /countdown/widget`. The four-step onboarding modal described in this prompt does not exist yet — it's new scope for this pass, not a redesign of something live.

## Prompt (copy-paste this into Claude Design)

```
Torrorèndum: Catalan voting competition for Torrons Vicens, warm brown/gold/cream palette, burgundy accent. Design three connective components as one family: (1) the top navigation bar with logo and links to Vota / Classificació / Estadístiques / Historial, (2) the countdown widget (currently counts to Jan 6 — extend it to show which phase is active: "dies fins al pas a eliminatòries" during open season, then "dies fins a la gran final" during the bracket), and (3) a four-step onboarding modal shown on first visit explaining how the two-phase competition works. All three should feel like the same object language — same corner radii, same iconography style — even though they appear in different places.
```
