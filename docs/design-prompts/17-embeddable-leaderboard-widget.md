# Prompt 17 — Embeddable leaderboard widget

**Set:** B (new screens for shipped features)
**Status:** Live at `GET /embed/leaderboard` (`public/templates/embed_leaderboard.html`). Shipped 2026-07-03.

## Prompt (copy-paste this into Claude Design)

```
Torrorèndum: Catalan voting competition for Torrons Vicens, warm brown/gold/cream palette, burgundy accent. Design a compact, embeddable version of the top-10 leaderboard meant to sit inside an iframe on someone else's website (a food blogger's site, a news article). It must render correctly with no assumptions about surrounding page styles — self-contained borders/background, no reliance on the host page's fonts or colors, and a small persistent "Torrorèndum" credit/link back to the source. Two size variants: a compact strip (top 3 only) and a fuller card (top 10). This is the site's ambassador on other people's pages — it needs to look finished and trustworthy in isolation.
```
