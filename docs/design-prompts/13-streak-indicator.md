# Prompt 13 — Streak indicator

**Set:** B (new screens for shipped features)
**Status:** Live as a small component on the vote flow and `GET /stats` (`CurrentStreak`/`LongestStreak` on the user record) — there's no dedicated route, it's a widget embedded in existing pages. Shipped 2026-07-03.

## Prompt (copy-paste this into Claude Design)

```
Torrorèndum: Catalan voting competition for Torrons Vicens, warm brown/gold/cream palette, burgundy accent. Design a small, persistent streak indicator component (not a full page) — a flame or similar warm-toned glyph plus a day count, shown near the top of the voting screen and on the stats page. Needs a satisfying "just extended" micro-state and a distinct "at risk, vote today" state, both readable at a glance without demanding attention. Keep it small enough to never compete with the duel cards for visual weight.
```
