# Prompt 05 — Leaderboard (ranked list)

**Set:** A (existing site, redesigned)
**Status:** Live at `GET /leaderboard` (`public/templates/leaderboard.html`).

## Prompt (copy-paste this into Claude Design)

```
Torrorèndum: Catalan voting competition for Torrons Vicens, warm brown/gold/cream palette, burgundy accent. Design the open-season leaderboard: a classic ranked list — rank, photo, name, ELO score, vote count, rank-change indicator (up/down/new), gold/silver/bronze treatment for the top 3 — toggleable between "els meus resultats" (personal) and "resultats globals" (community). This is distinct from the bracket view (a separate screen, see Prompt 10) — this one is the ongoing, ever-shifting live ranking during the open weeks. Design it to survive being screenshotted on its own; assume someone crops just the top 3. Caramel-gold for medal treatments, burgundy reserved for the single current #1.
```
