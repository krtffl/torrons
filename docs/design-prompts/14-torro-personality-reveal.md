# Prompt 14 — "Torró personality" reveal

**Set:** B (new screens for shipped features)
**Status:** **Not built.** Explicitly deferred when the share-card engine shipped (2026-07-03) — only the plain rank/vote-count card went out; the persona/percentile copy ("Ets del 8% que prefereix...") needs real copywriting work this repo hasn't done yet. See the TODO in `internal/sharecard/sharecard.go`. This is genuinely new design work, not a reskin.

## Prompt (copy-paste this into Claude Design)

```
Torrorèndum: Catalan voting competition for Torrons Vicens, warm brown/gold/cream palette, burgundy accent. Design a mid-campaign reveal moment shown once a user has voted enough: instead of a raw leaderboard, a persona/percentile statement ("Ets del 8% que prefereix els sabors més atrevits") paired with the user's top torró. This should feel like a small ceremony — a distinct full-screen moment with its own pacing, not an inline banner — and should visually set up the downloadable share card in Prompt 15, since this reveal is the moment a user decides whether to share.
```
