# Prompt 16 — Wrapped recap

**Set:** B (new screens for shipped features)
**Status:** Live (plumbing only, same plain-v1 treatment as Prompt 15) at `GET /wrapped` and `GET /wrapped/card.png` (`public/templates/wrapped.html`, `internal/sharecard/wrapped.go`). Shipped 2026-07-05. Real content will stay sparse until an actual campaign runs (December), but the layout/engine is ready for a visual pass now.

## Prompt (copy-paste this into Claude Design)

```
Torrorèndum: Catalan voting competition for Torrons Vicens, warm brown/gold/cream palette, burgundy accent. Design "Torrorèndum Wrapped" — a campaign-end, multi-section recap page in a vertical scroll or slide format: total votes cast this season, the user's most contested duel (closest vote of theirs), their most "unpopular" pick versus the crowd, their path through the Gran Final bracket, and a final shareable summary card (reusing Prompt 15's template). Each section should feel like its own beat with a brief pause before the next, in the spirit of Spotify Wrapped or Duolingo's year-in-review, ending on the single most shareable slide, not the driest statistic.
```
